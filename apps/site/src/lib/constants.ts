/** Single source of truth for the contact address shown across the site --
 * change it once here, not at each call site. */
export const CONTACT_EMAIL = "samhf718@gmail.com";

/** The landing page's <title>. Shared so `og:title` is guaranteed to match
 * it rather than being a second copy that can silently drift. */
export const SITE_TITLE = "Waysafe — the required signer for agent payments";
