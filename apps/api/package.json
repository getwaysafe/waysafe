{
  "name": "@agentpay/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "compile": "tsx src/cli.ts",
    "compile:record": "tsx src/record-fixture.ts",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentpay/core": "*",
    "fastify": "^5.2.0",
    "zod": "^3.24.1"
  }
}
