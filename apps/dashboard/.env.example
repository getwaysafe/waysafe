# The Waysafe API this dashboard reads from.
WAYSAFE_API_BASE_URL="http://localhost:3001"

# Encrypts the session cookie (D-23 / OQ-4). Generate with:
#   openssl rand -base64 32
WAYSAFE_DASHBOARD_SESSION_SECRET=""
