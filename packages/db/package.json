{
  "name": "@waysafe/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "constraints": "prisma db execute --file prisma/manual-constraints.sql --schema prisma/schema.prisma",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
