# --- Database ---
DATABASE_URL="postgresql://user:password@localhost:5432/agentpay?schema=public"

# --- Intent compiler (LLM is used ONLY to compile natural language into policy) ---
ANTHROPIC_API_KEY=""
AGENTPAY_COMPILER_MODEL="claude-sonnet-4-5"
# Set to "fixture" to replay recorded compiler output (no network, used in tests)
AGENTPAY_COMPILER="anthropic"

# --- Payments (Week 4) ---
STRIPE_SECRET_KEY="sk_test_..."

# --- Evidence signing (Week 3) ---
# Ed25519 private key, base64 PKCS8. Generate: npm run keygen -w @agentpay/api
AGENTPAY_EVIDENCE_SIGNING_KEY=""

# --- WebAuthn (Week 3) ---
# Passkeys are bound to this domain. Changing it invalidates every registered passkey.
AGENTPAY_RP_ID="localhost"
AGENTPAY_RP_ORIGIN="http://localhost:3000"

# --- API ---
PORT="3001"
